# 要件定義残タスク

最終更新日：2026-07-26

このドキュメントは、基本設計に進む前に要件定義段階で未完／不足している項目をまとめたタスクリストです。

技術スタックに関する決定事項は `技術スタック提案.md` に集約しています。本ドキュメントには**未決の項目のみ**を残します。

---

## A. 基本設計に必要な未完事項（要件定義残タスク）
以下は必須かつ優先度高の項目です。基本設計着手前に決定・追記してください。

1. 画面設計・UI要件
   - 主要画面のワイヤーフレーム（メイン画面、予約作成、予約詳細、マイページ、管理画面等）
   - 画面遷移図（ログイン/未ログイン時の遷移含む）
   - スイムレーン表示の詳細仕様（表示列数、時間範囲の変更可否、スクロール挙動、ズーム単位）

2. ビジネスルールの詳細化
   - 予約のキャンセル可能期限・キャンセル料（ルールがある場合）
   - 参加者の追加/削除可能期間（予約直後〜何時間前まで等）
   - 同一ユーザーの重複予約禁止ルール（同一時間帯の複数予約可否）
   - チーム枠ルールの厳密定義（チーム数上限、チーム参加の優先順序）
   - 拠点スタッフが行う「利用不可設定」の副作用（既存予約の扱い）

3. 権限制御（詳細マトリクス）
   - 機能ごとのCRUD権限マトリクス（メンバー／チームリーダー／拠点スタッフ／管理者）
   - 拠点スタッフ間の共有アカウント運用ルールと操作ログの紐付け方法

4. データ設計（概要レベル）
   - ER図（主要エンティティ：User, Team, Court, Venue, Reservation, Participation, AuditLog等）
   - 主要テーブルの想定カラム（主キー、外部キー、主なインデックス）
   - 保持期間ポリシー（予約履歴、ログなど）と削除フロー

5. API設計（概要）
   - 主要エンドポイント一覧（例：/api/reservations, /api/venues, /api/users 等）
   - リクエスト/レスポンス仕様（認証方式、共通エラーフォーマット）
   - ページネーション・フィルタ仕様

6. ワークフロー／ビジネスプロセス図
   - 予約登録〜確定〜実施〜完了の状態遷移図
   - チーム申請／承認フロー（誰が承認、通知のタイミング）
   - 参加申請の承認フロー（自動承認 or リーダー承認など）

7. エラーコード設計
   - 共通エラーコード一覧（例：E001=認証エラー、E010=定員超過 等）
   - クライアント向け/管理者向けのメッセージ方針

8. 非機能要件の明確化（数値化）
   - ~~最終的な同時接続目標、SLA、応答時間、スループット~~ → 確定（`アプリケーション要求機能.md`）
     - SLA 99.9%（計画メンテナンス除く）、定期メンテナンスは週1回 0:00〜5:00
   - **RTO / RPO の目標値**（未決）
     - 案1（単一VM）の実測見込みは RTO 数時間・RPO 最大24時間。この値を踏まえて目標を設定する
   - **メンテナンス実施曜日**（未決。火〜木の早朝を推奨）
   - **メンテナンス実施日のバックアップ（03:00）と作業の順序**（未決）

9. テスト計画（基本）
   - 必須の自動テスト範囲（ユニット、統合、E2E）と使用するフレームワーク
   - パフォーマンステストの条件（負荷/同時接続シナリオ）

10. 運用ポリシー
   - サポート受け付け方法（メールのみ、対応目安）
   - サービス終了時のユーザーデータ取り扱い方針

---

## B. 技術スタックの残課題

決定済みの項目は `技術スタック提案.md` に移しました。ここには未決のものだけを残しています。

### B-0. 決定済み（2026-07-26、詳細は `技術スタック提案.md`）

本番構成は**案1：Windows Server 単一VM 全部入り**に決定。以下は解決済みのため、本ドキュメントでは扱いません。

- 本番ホスティング先 / 本番OS / Docker 化の可否（Docker は不採用）
- Node.js バージョン（22 LTS、`engines` で固定）と TypeScript 採用
- DB 運用方針（PostgreSQL セルフホスト、Prisma、マイグレーション運用ルール、バックアップ/リストア手順）
- CI/CD（GitHub Actions + Windows セルフホストランナー）とデプロイフロー
- 環境変数・Secrets の管理（`.env.local` / GitHub Secrets / NTFS ACL）
- 監視（Zabbix・エージェント配置・監視項目）とログ運用（ローテーション、5世代、監査180日）
- キャッシュ戦略（Redis は不採用）
- 画像の保存先（データディスク。将来の移行に備え保存先を差し替え可能な層を挟む）
- TLS 証明書管理（win-acme による自動更新）
- 概算コスト（案1の月額目安）

### B-1. 認証方式・セッション管理（優先度：高）
- NextAuth.js を採用するか、独自実装にするか
- **制約：二重ログイン後勝ちの要件により、サーバー側セッション（DBセッション方式）が必須**。ステートレスJWT単独では既存トークンを無効化できない
- セッション保存先（Redis 不採用のため PostgreSQL を推奨）
- OAuth（Google/Facebook）を使うか否か（`アプリケーション要求機能.md` に記載がないため、原則不要と考えられる。要確認）

### B-2. メール送信基盤（優先度：高）
- プロバイダ決定：Amazon SES（従量・安価）/ SendGrid（無料枠後 月$20程度）
- 送信上限・レート制限への対策
- 送信失敗時の3回リトライとログ記録の実装方式
- プッシュ通知は**実装しない**（要求機能.md「記載されていない通知機能」に該当）

### B-3. DB 接続・性能（優先度：中）
- コネクションプーリング戦略（Prisma の `connection_limit` と PostgreSQL の `max_connections` の突き合わせ）
- 単一VM構成のためアプリとDBが同居する。メモリ配分（`shared_buffers` 等）の方針

### B-4. セキュリティ運用（優先度：中）
- SAST/DAST の具体ツール選定（Dependabot / Snyk / Semgrep 等）
- レート制限の実装方式（ログイン / パスワードリセット / 予約登録）
- CAPTCHA 等のボット対策を入れるか
- 初回リリース前のペネトレーションテストの実施方法・委託先

### B-5. 開発・テスト支援ツール（優先度：中）
- 単体テスト：Jest + React Testing Library の採用可否（**未導入**）
- E2E：Playwright の採用可否（**未導入**）
- テストDBの構築方針（`migrate deploy` で構築すること。`db push` では手書きSQLの制約が入らない）

### B-6. 運用スクリプト・自動化（優先度：中）
- 予約データの日付経過削除、退会ユーザー削除のバッチ実装（タスクスケジューラから起動）
- 紛争解決用データ（12ヶ月保持）の退避処理
- マイグレーション自動実行をどこまで自動化するか（破壊的変更は人の承認を挟む）

### B-7. 将来検討（優先度：低）
- ログ集約（現状は単一VM上のローカル保持で足りる）
- CDN による静的アセット配信
- 画像変換・サムネイル生成フロー
- 案2（マネージドPostgreSQLへの移行）の実施判断基準

---

## C. 次アクション（推奨）
- 優先度高：**認証方式**（B-1）と**メール送信プロバイダ**（B-2）を決定する
- 優先度高：A章の画面設計・ビジネスルール詳細化・権限マトリクス・ER図を作成する
- 優先度中：RTO/RPO 目標値とメンテナンス実施曜日を確定する（A-8）
- 優先度中：テストフレームワークを導入し、CI に組み込む（B-5）
- 優先度低：CDN / 画像処理最適化、案2への移行判断基準（B-7）

---

作成者：GitHub Copilot の支援により自動生成（内容は要確認）
2026-07-26：本番構成の決定（案1）に伴い、B章の決定済み項目を `技術スタック提案.md` へ移動
