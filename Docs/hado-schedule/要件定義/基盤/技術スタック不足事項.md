# 技術スタック不足事項

## 概要

「技術スタック整理.md」で定義された Next.js + PostgreSQL + Prisma スタックをベースに、基本設計に必要な技術的な不足事項をまとめています。

---

## 1. フロントエンド実装の詳細

### 1.1 UI フレームワーク・コンポーネント
- [ ] **CSS フレームワーク**
  - 現在：Tailwind CSS（記載あり）
  - 確認項目：
    - Tailwind CSS のバージョン
    - プラグイン（forms, typography 等）の選定
    - カスタムテーマの定義

- [ ] **UI コンポーネントライブラリ**
  - 選定肢：Shadcn/ui, Radix UI, Material-UI, Ant Design 等
  - 決定：**選定なし** ← 不足
  - 推奨：Shadcn/ui（Tailwind CSS ベース、Radix UI 使用）

### 1.2 状態管理
- [ ] **必要性の検討**
  - 大規模な共有状態があるか？
  - React Context で十分か？Redux/Zustand 必要か？
  - 決定：**未決定** ← 不足

- [ ] **キャッシング・サーバー状態管理**
  - React Query / SWR / Apollo Client
  - 決定：**未決定** ← 不足
  - 推奨：React Query（API ベースの状態管理に最適）

### 1.3 フォーム管理
- [ ] **フォーム ライブラリ**
  - React Hook Form / Formik
  - 決定：**未決定** ← 不足
  - 推奨：React Hook Form（軽量、パフォーマンス良好）

### 1.4 日付・時刻処理
- [ ] **ライブラリ**
  - dayjs / date-fns / moment.js
  - 決定：**未決定** ← 不足
  - 推奨：dayjs（軽量、日本語対応）

### 1.5 グラフ・カレンダー表示
- [ ] **カレンダー ライブラリ**
  - react-big-calendar / react-calendar / fullcalendar
  - 決定：**未決定** ← 不足
  - 推奨：react-big-calendar または fullcalendar（スイムレーン表示に対応）

### 1.6 ページネーション・テーブル
- [ ] **テーブル ライブラリ**
  - Tanstack Table (React Table) / ag-grid
  - 決定：**未決定** ← 不足
  - 推奨：Tanstack Table（軽量、カスタマイズ性高）

### 1.7 多言語対応
- [ ] **i18n ライブラリ**
  - i18next / react-intl
  - 決定：**未決定** ← 不足
  - 要件再確認：「言語はブラウザ設定に依存」とあるが、UI 翻訳は必要か？

---

## 2. バックエンド実装の詳細

### 2.1 API フレームワーク・ルーティング
- [ ] **Next.js API Routes の使用**
  - 決定：Next.js App Router + Route Handlers（推定）
  - 確認項目：
    - App Router vs Pages Router（おそらく App Router）
    - ミドルウェア設定（認証、CORS）
    - エラーハンドリング戦略

### 2.2 認証・認可
- [ ] **認証ライブラリ**
  - NextAuth.js / jsonwebtoken 直実装
  - 決定：**未決定** ← 不足
  - 推奨：NextAuth.js（統合度が高い）

- [ ] **認可ロジック**
  - ロールベースアクセス制御（RBAC）の実装方法
  - ミドルウェアでチェック？コンポーネントレベルでチェック？

### 2.3 Prisma ORM
- [ ] **確認項目**
  - schema.prisma ファイルの作成
  - マイグレーション戦略（SQL or Prisma Migrate）
  - 種子データ（seeding）の準備
  - 決定：**記載あり（基本のみ）** ← 詳細不足

### 2.4 バリデーション
- [ ] **ライブラリ**
  - Zod / Joi / Yup
  - 決定：**未決定** ← 不足
  - 推奨：Zod（TypeScript 統合が優れている）

### 2.5 ロギング
- [ ] **ロギング ライブラリ**
  - winston / pino / bunyan
  - 決定：**未決定** ← 不足
  - 推奨：pino（パフォーマンス良好）

### 2.6 エラーハンドリング
- [ ] **グローバルエラーハンドリング**
  - 統一されたエラーレスポンス形式の定義
  - 決定：**未決定** ← 不足

### 2.7 キャッシング
- [ ] **キャッシング戦略**
  - Redis？メモリキャッシュ？
  - TTL の設定
  - 決定：**未決定** ← 不足
  - 推奨：Redis（スケーラビリティが必要な場合）

### 2.8 メール送信
- [ ] **メール ライブラリ**
  - Nodemailer / SendGrid / Mailgun 等
  - 決定：**未決定** ← 不足
  - 推奨：NodeMailer（軽量） or SendGrid（信頼性重視）

### 2.9 ファイルアップロード
- [ ] **ライブラリ**
  - multer / formidable
  - ストレージ：ローカルディスク？S3？
  - 決定：**未決定** ← 不足

### 2.10 スケジュール処理
- [ ] **予約データ物理削除の自動化**
  - node-cron / bull (job queue)
  - 決定：**未決定** ← 不足
  - 推奨：node-cron（シンプル） or bull（信頼性重視）

### 2.11 WebSocket（リアルタイム通知）
- [ ] **必要性の検討**
  - 予約が登録されたらリアルタイムに通知する？
  - Socket.io か Native WebSocket か？
  - 決定：**要件に記載なし** ← 明確化必要

---

## 3. データベース実装の詳細

### 3.1 PostgreSQL バージョン・設定
- [ ] **バージョン**
  - 推奨：PostgreSQL 15 以上
  - 決定：**未決定** ← 不足

- [ ] **接続プーリング**
  - PgBouncer / Prisma Connection Pooling
  - 決定：**未決定** ← 不足
  - 推奨：Prisma の組み込みプーリング（開発）、本番環境では PgBouncer

### 3.2 インデックス戦略
- [ ] **パフォーマンス最適化**
  - 頻繁にアクセスされるカラムへのインデックス
  - 複合インデックスの定義
  - 決定：**未決定** ← ER図作成後に決定

### 3.3 拡張機能
- [ ] **UUID の使用**
  - UUID v4 for Primary Keys？BIGSERIAL？
  - 決定：**未決定** ← 不足
  - 推奨：UUID v4（分散性重視）

- [ ] **Soft Delete**
  - 削除フラグの実装か物理削除か？
  - 要件：「物理削除」と記載 → 確認必要

---

## 4. 開発環境・ツール

### 4.1 リント・フォーマッター
- [ ] **ESLint**
  - 既存の eslint.config.mjs から確認
  - 追加ルール：必要か？

- [ ] **Prettier**
  - インストール確認
  - 決定：**不確定** ← 要確認

### 4.2 テスティング
- [ ] **ユニット テスト**
  - Jest / Vitest
  - 決定：**未決定** ← 不足

- [ ] **統合 テスト**
  - Testing Library
  - 決定：**未決定** ← 不足

- [ ] **E2E テスト**
  - Cypress / Playwright / Puppeteer
  - 決定：**未決定** ← 不足
  - 推奨：Playwright（プロダクション品質）

### 4.3 デバッギング・ログ
- [ ] **開発環境での debug ツール**
  - Redux DevTools（状態管理使用時）
  - Chrome DevTools
  - 決定：**デフォルト** ← 明示的に記載なし

### 4.4 環境変数管理
- [ ] **既存：.env.local**
  - 確認内容：
    - 開発/テスト/本番環境の分離方法
    - シークレット管理の方針

---

## 5. インフラストラクチャ・デプロイメント

### 5.1 ホスティング環境
- [ ] **選定**
  - AWS / GCP / Azure / Vercel（Next.js）/ Heroku 等
  - 決定：**未決定** ← 不足
  - 推奨：Vercel（Next.js 最適化）、自前ホスティング不要

### 5.2 データベースホスティング
- [ ] **選定**
  - AWS RDS / Google Cloud SQL / Railway / Supabase 等
  - 決定：**未決定** ← 不足
  - 推奨：Supabase（PostgreSQL マネージド、コスト低）

### 5.3 Docker コンテナ化
- [ ] **Dockerfile の作成**
  - 開発環境用 / 本番環境用
  - 決定：**未決定** ← 不足
  - 推奨：マルチステージビルド

### 5.4 ローカル開発環境の再現性
- [ ] **Docker Compose**
  - PostgreSQL + Next.js の定義
  - 決定：**未決定** ← 不足

### 5.5 監視・ロギング（本番環境）
- [ ] **ロギングサービス**
  - Datadog / New Relic / LogRocket / Sentry
  - 決定：**未決定** ← 不足

- [ ] **エラートラッキング**
  - Sentry / Rollbar
  - 決定：**未決定** ← 不足

---

## 6. セキュリティ

### 6.1 依存関係の脆弱性スキャン
- [ ] **npm audit**
  - 定期実行の仕組み
  - GitHub Dependabot の設定

- [ ] **Snyk / WhiteSource**
  - 自動スキャンの検討
  - 決定：**未決定** ← 不足

### 6.2 HTTPS / SSL 証明書
- [ ] **Let's Encrypt**
  - 自動更新の仕組み
  - 決定：**Vercel 使用時は自動** ← ホスティング決定後確認

### 6.3 CORS 設定
- [ ] **ホワイトリスト**
  - フロントエンドのドメイン制限
  - 決定：**未決定** ← API 設計時に決定

### 6.4 Rate Limiting
- [ ] **API エンドポイントの保護**
  - express-rate-limit 等
  - 決定：**未決定** ← 不足
  - 推奨：実装必須（ブルートフォース攻撃対策）

### 6.5 CSRF 対策
- [ ] **トークンベースの保護**
  - Next.js での実装方法
  - 決定：**未決定** ← 不足

---

## 7. パフォーマンス最適化

### 7.1 フロントエンド
- [ ] **コード分割**
  - Next.js dynamic import / React.lazy
  - 決定：**基本的には自動** ← 手動最適化必要箇所の検討

- [ ] **画像最適化**
  - Next.js Image コンポーネント
  - WebP 変換
  - 決定：**基本的には自動** ← 確認

- [ ] **バンドルサイズ分析**
  - webpack-bundle-analyzer / size-limit
  - 決定：**未決定** ← 不足

### 7.2 バックエンド
- [ ] **データベースクエリ最適化**
  - N+1 問題の回避
  - インデックス戦略
  - 決定：**ER図作成後に検討** ← 不足

- [ ] **API レスポンスの最小化**
  - GraphQL vs REST の検討（現在 REST）
  - 決定：**REST** ← 確定

---

## 8. GitHub / Git 管理

### 8.1 リポジトリ設定
- [ ] **ブランチ保護ルール**
  - main ブランチ：プルリクエストレビュー必須
  - 自動テスト実行後にのみマージ可

- [ ] **CI/CD 統合**
  - GitHub Actions の設定

- [ ] **Issue / PR テンプレート**
  - スタンダード化

- [ ] **コードレビュー基準**
  - レビュアー数
  - 承認者

- [ ] **コミットメッセージ規約**
  - Conventional Commits

- [ ] 決定：**未決定** ← 不足

---

## 9. ドキュメント生成

### 9.1 API ドキュメント
- [ ] **Swagger / OpenAPI**
  - swagger-jsdoc / ts-swagger
  - 決定：**未決定** ← 不足
  - 推奨：swagger-jsdoc（Next.js との統合が簡単）

### 9.2 Storybook（UI コンポーネント）
- [ ] **検討**
  - コンポーネントの可視化・再利用性向上
  - 決定：**検討対象** ← 規模に応じて決定

---

## 10. 本番環境へのチェックリスト

### 10.1 デプロイ前チェック
- [ ] すべてのテストがパス
- [ ] Linting エラーなし
- [ ] パフォーマンスベンチマーク達成
- [ ] セキュリティスキャン完了
- [ ] データベースマイグレーション テスト済み
- [ ] 環境変数すべて設定済み
- [ ] バックアップ・復旧手順確認
- [ ] ローリングバック手順確認
- [ ] 監視・ロギング設定完了
- [ ] 本番 DB の初期データ準備完了

---

## 技術スタック最終確認表

| 項目 | 選定 | 状態 |
|------|------|------|
| **フロントエンド** | | |
| フレームワーク | Next.js 14+ | ✅ |
| UI ライブラリ | **未定** | ⏳ |
| CSS | Tailwind CSS | ✅ |
| 状態管理 | **未定** | ⏳ |
| HTTP クライアント | **未定** | ⏳ |
| フォーム管理 | **未定** | ⏳ |
| **バックエンド** | | |
| API フレームワーク | Next.js Routes | ✅ |
| ORM | Prisma | ✅ |
| 認証 | **未定** | ⏳ |
| バリデーション | **未定** | ⏳ |
| ロギング | **未定** | ⏳ |
| **データベース** | | |
| DBMS | PostgreSQL | ✅ |
| バージョン | **未定** | ⏳ |
| **その他** | | |
| テスティング | **未定** | ⏳ |
| CI/CD | GitHub Actions | ⏳ |
| ホスティング | **未定** | ⏳ |
| 監視 | Zabbix + **未定** | ⏳ |

---

## 推奨される次のステップ

1. **UI フレームワーク**を Shadcn/ui に決定
2. **状態管理**を React Query に決定
3. **認証**を NextAuth.js に決定
4. **テスティング**戦略を決定（Jest + Testing Library + Playwright）
5. **ホスティング**を Vercel に決定
6. **データベースホスティング**を Supabase に決定
7. **メール送信**を SendGrid に決定
8. **各ライブラリの package.json への追加**
9. **開発環境の Docker Compose 設定**
10. **初期セットアップとプロトタイプ実装**

---

## 更新履歴

| 日付 | 更新内容 |
|------|---------|
| 2026-05-20 | 初版作成 |
